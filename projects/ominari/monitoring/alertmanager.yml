global:
  resolve_timeout: 5m
receivers:
- name: default
  webhook_configs:
  - send_resolved: true
    url: http://localhost:8888/api/alerts
route:
  group_by:
  - alertname
  - cluster
  - service
  group_interval: 10s
  group_wait: 10s
  receiver: default
  repeat_interval: 12h
