# Install dependencies
install:
    uv sync

# Run linting
lint:
    uv run ruff check .

# Format code
format:
    uv run ruff format .

# Check linting and formatting
check:
    uv run ruff check .
    uv run ruff format --check .
